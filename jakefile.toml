test = "uv run pytest tests/"
format = "uv run ruff format ."
lint = "uv run ruff check --fix ."
typecheck = "uv run ty check src/better_result"
format-check = "uv run ruff format --check ."
lint-check = "uv run ruff check ."
